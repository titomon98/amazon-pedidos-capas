Estoy utilizando arquitectura en capas en NESTJS y este funciona como un microservicio con base de datos mySQL. En el mismo quiero recibir los productos que van a estar dentro de un pedido, pero no se aun que pueda venir del microservicio de pedidos.



Ya tengo diseñados estos modelos:



import {
    Entity,
    PrimaryGeneratedColumn,
    Column,
    CreateDateColumn,
    UpdateDateColumn,
    OneToMany
} from 'typeorm'

import { Status } from '../../common/enums/status.enum'
import { DetallePedido } from './detalle-pedido.model'

@Entity('pedidos') //como se llama la tabla
export class Pedido {
    @PrimaryGeneratedColumn()
    id: number

    @Column({
        type: 'int', 
        default: Status.PENDIENTE_PAGO
    })
    pedido_status: number

    @Column({
        type: 'decimal',
        precision: 2,
        scale: 2,
        default: 0.0
    })
    pedido_total: number

    @CreateDateColumn()
    createdAt: Date

    @UpdateDateColumn()
    updatedAt: Date

    @OneToMany(() => DetallePedido, (item) => item.pedido, {
        cascade: true,
        eager: true
    })
    items: DetallePedido[]
}


import {
    Entity,
    PrimaryGeneratedColumn,
    Column,
    CreateDateColumn,
    UpdateDateColumn,
    ManyToOne,
    JoinColumn
} from 'typeorm'

import { Status } from '../../common/enums/status.enum'
import { Pedido } from './pedidos.model'

@Entity('detalle_pedidos')
export class DetallePedido {
    @PrimaryGeneratedColumn()
    id: number

    @Column()
    pedido_id: number

    @Column()
    producto_id: number

    @Column()
    cantidad: number

    @Column()
    descripcion: string
    
    @Column({
        type: 'decimal',
        precision: 2,
        scale: 2,
        default: 0.0
    })
    precio: number

    @Column({
        type: 'decimal',
        precision: 2,
        scale: 2,
        default: 0.0
    })
    subtotal: number

    @CreateDateColumn()
    createdAt: Date

    @UpdateDateColumn()
    updatedAt: Date

    @ManyToOne(() => Pedido, (pedido) => pedido.items, { onDelete: 'CASCADE'})
    @JoinColumn({ name: 'pedido_id' })
    pedido: Pedido;

}



Tambien tengo un ENUM para cosas generales de mi aplicacion, el mismo es el siguiente:



export enum Status {
    CANCELADO = 0,
    ENTREGADO = 1,
    PENDIENTE_PAGO = 2,
    NO_EXISTENCIA_DISPONIBLE = 3,
    PAGADO = 4,
    ENVIADO = 5,
    PERDIDO = 6
}



Necesito que me ayudes a crear los repositorios para esta aplicacion. Luego te pedire servicios, controllers y rutas, pero eso sera despues. Ahorita apegate a la instruccion.